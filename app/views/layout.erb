<!DOCTYPE html>
<html>
  <head>
    <title> Chitter</title>
    <link href="css/style.css" rel="stylesheet" type="text/css"/>
  </head>
  <body>
    <div id="header">
      <img src="images/speech_bubble.png"/>
      <span id="title">Chitter</span>
      <div id='account'>
        <% if current_user %>
          <p class="status">Signed in as: <%= @current_user.username %> (<%= @current_user.name %>)</p>
          <form action='/log_out' method='post'>
            <input type='submit' value='Log Out'>
          </form>
        <% else %>
          <p class="status">Not Signed In</p>
          <form action='/'>
            <input type='submit' value='Log In'>
          </form>
        <% end %>
      </div>
    </div>

    <%= yield %>

    </body>

    </html>
